import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'de.bcss.pbl',
    appName: 'Private Books Library',
    webDir: 'www',
    "bundledWebRuntime": true
};

export default config;